# CLAUDE.md — AI News プロジェクト指示書

## セキュリティポリシー（最優先）

### 外部パッケージ・スクリプトの実行禁止

- `npx` で外部スキルやパッケージを**絶対にインストール・実行しない**（例: `npx skills add ...`）
- 信頼できる公式パッケージマネージャ（npm, yarn, pnpm）からの**公式かつ著名なパッケージのみ**許可する（例: vite, react, eslint 等）
- 個人リポジトリや無名のパッケージは、ユーザーが「入れてみて」と言っても**実行せず、まずリスクを説明する**
- SKILL.md、scripts/、postinstall スクリプト等の**中身が事前に確認できないものは実行しない**

### 外部コードの検証義務

- 外部から提供されたコマンド、スクリプト、URL は**必ず中身を確認してからユーザーに説明する**
- 「おすすめです」「便利です」「安全です」という推薦だけでは信頼しない
- GitHub リポジトリのコードであっても、スター数や推薦者に関係なく**中身を読んでから判断する**
- ユーザーが興奮して「すぐ入れて」と言っても、検証プロセスを省略しない

### 実行前の警告義務

以下の操作を行う前に、**必ずリスクをユーザーに説明し、明示的な承認を得る**:

- 外部パッケージのインストール（npm install, yarn add 等）
- 外部URLへのデータ送信
- 環境変数・認証情報に触れる操作
- シェルスクリプトの実行
- postinstall / preinstall フックを含むパッケージの導入
- curl, wget 等による外部リソースの取得と実行

### 認証情報の保護

- `.env`、API キー、トークン、パスワードを含むファイルを**絶対にコミット・表示・外部送信しない**
- 認証情報が含まれる可能性のあるファイルを発見した場合、即座にユーザーに警告する
- git add -A や git add . を使わず、**ファイルを個別に指定してステージングする**

### サプライチェーン攻撃への警戒

- 依存パッケージの追加時は、パッケージ名のタイポスクワッティングに注意する
- package.json に存在しない新規依存の追加は、ユーザーの明示的な承認なしに行わない
- lock ファイル（package-lock.json, yarn.lock）の意図しない変更に注意する

## 編集ポリシー

### 記事の編集ルール

- 公開済み記事の内容を**無断で変更しない**
- 編集を行う場合は必ず**編集履歴セクション**に変更内容を記録する
- 事実関係が確認できない情報は掲載しない
- 特定個人への帰属は**検証可能な一次ソースがある場合のみ**記載する
- 速報は後日、分析記事へ再構成することがある（その旨を編集履歴に明記）

### ソース検証

- 一次ソース（公式発表、本人の投稿、GitHub Issue 等）を優先する
- SNS の投稿を引用する場合、アカウントの実在と投稿内容を可能な限り検証する
- 検証できないソースは「未確認情報」として明記するか、掲載しない

### セキュリティ記事の追加レビュー義務

セキュリティに関する対策・手順を記事に書く場合、以下の観点で**自己検証を必ず行う**:

- **順序の妥当性**: 「インストール後に確認」のように、実行順序が逆転していないか。npm install は postinstall を自動実行するため、**インストール前の確認**が必要
- **対象読者の現実性**: 「毎回AIに聞け」等、初心者が実際にやるかを考える。人間の注意力に依存する対策は防御として不十分
- **バイブコーディングとの整合性**: AIエージェントが自律的にパッケージ導入・スクリプト実行する場面を想定しているか。CLAUDE.md 等の仕組みによる自動防御を優先する
- **ソース不明コマンドへの警告**: `npx xxx` や `curl | sh` で GitHub リポジトリや公式サイトへのリンクがない場合、その危険性を明記しているか
- **防御策の実行可能性**: 読者のスキルレベル別に、実際に実行できる具体的な手順を示しているか

### 社会的文脈の付加と印象操作の防止

AI News の強みは、技術ニュースに**検証可能な社会的文脈**を組み合わせて読者に判断材料を提供する点にある。
ただし、文脈の付加と印象操作は紙一重であるため、以下のルールを厳守する。

**推奨: 事実の並列提示（読者が判断できる材料を揃える）**
- 例: 「RTranslator は完全ローカル動作で音声データを外部送信しない。一方 DeepL は ISO 27001 認証を取得しクラウド処理でもデータ保護を担保している」
- 複数の検証可能な事実を並べ、読者自身が比較・判断できるようにする
- 各事実には出典（公式サイト、認証情報、GitHub リポジトリ等）を明示する

**禁止: 暗示による印象操作**
- 事実Aと事実Bを並べることで、**明示していない結論C**を読者に想起させる構成にしない
- 「〜と言われている」「〜の懸念が広がっている」を主語・出典なしに使わない
- 特定の企業・製品を不当に有利または不利に見せる文脈の配置をしない
- 見出しや太字で一方の事実だけを強調し、反対の事実を目立たない位置に置かない

**チェックポイント:**
- その段落を読んだ読者が「筆者はXを批判/推奨している」と感じるなら、構成を見直す
- 書いた文脈情報を削除しても記事の主旨が変わらないなら、その文脈は不要（装飾的印象操作の恐れ）
- 文脈情報を加えることで記事の理解が深まるなら、それは有益な文脈付加である

### 公開前の個人情報チェック（CI で強制）

記事本文にサイト運営者の名前を書いたまま push しかけた事故（2026年8月、プレビューの目視で発見）を受け、
**人間の目視に依存しない機械的な関門**を公開経路に置いた。

**`node scripts/check-private-info.mjs`**（`--dist` でビルド成果物も検査）

| 判定 | 対象 |
|---|---|
| **Error** | リポジトリ所有者名、環境固有パス（`/home/x/`・`/Users/x/`・`C:\Users\x`）、作業セッション/成果物の URL、資格情報らしき文字列、秘密鍵ブロック、`PRIVATE_TERMS` に登録した語 |
| **Warn** | 記事内のメールアドレス（レビュー済みのものは `scripts/allowed-emails.txt` に理由付きで登録すると除外） |

**設計上の要点**

- **このスクリプト自身に個人情報を書かない。** 検出対象をハードコードすると、それ自体がリポジトリへの記載になる。
  所有者名は実行時に導出し（CI は `GITHUB_REPOSITORY`、ローカルは `git remote`）、
  それ以外の固有語は Secrets の `PRIVATE_TERMS` から渡す。ローカルは gitignore 済みの `.private-terms`
- **サイト自身の公開 URL は打ち消す。** GitHub Pages の URL には所有者名が必ず入るため、
  `<owner>.github.io` と `github.com/<owner>` を照合前に除外する。これを入れないと
  `feed.xml` / `sitemap.xml` / canonical URL が毎回引っかかり、デプロイが常に落ちる
- **メールアドレスを一律 Error にしない。** サプライチェーン攻撃の記事で攻撃者のアドレスを
  取材対象として引用している実例がある。Warn に落とし、レビュー済みは allowlist で外す

**どこで走るか**

- `.github/workflows/checks.yml` — **PR と main への push の両方**（これ以前、PR には CI が一切無かった）
- `.github/workflows/deploy.yml` — build の前後。**ここが落ちれば deploy まで進まない**ため、
  個人情報を含んだまま公開されることはない

**運用**

- 記事に意図的にメールアドレスを載せる場合は、`scripts/allowed-emails.txt` に**なぜ必要かを添えて**登録する。
  運営者・関係者の連絡先をここに追加してはならない
- 検出語を増やしたい場合は Secrets の `PRIVATE_TERMS`（改行またはカンマ区切り）に追加する。
  **スクリプトやコミットに語そのものを書かないこと**

### 調査と検証の標準手順

大規模な調査（特集記事の材料集め、棚卸、複数社の横断比較）を行う場合、
**調査役と検証役を分離する**。以下は2026年8月に文献を確認したうえで確定した手順である。

**L0: 軸を決める前に、制約なしで一度広く当たる**

L1〜L4 は「集めたものが正しいか」「範囲が足りているか」を見る。
しかし**軸の切り方そのものが妥当か**は、どの層も見ていない。ここが穴だった。

2026年8月、同じ形の取りこぼしを1日に2回起こしている。

| 事例 | 何を確認したか | 何を見落としたか |
|---|---|---|
| Devin Desktop | 運営元（Cognition）とブランド統合（6/2）は確認した | **中核機能 Cascade が7/1に提供終了**していた。現行は Devin Local |
| NEC | 「AI駆動開発の標準があるか」という枠で探した | **枠の外にあった本命**（8/10 発表の AI 自律型組織）を取りこぼした |

原因は共通している。**軸を先に決めると、軸の外にある本命を構造的に見落とす。**
前者は「資本関係・ブランド・料金」という軸を立てたため機能の世代交代が視野に入らず、
後者は「開発標準」という軸を立てたため組織施策が視野に入らなかった。

**したがって、軸を確定する前に次を行う。**

1. 対象ごとに「**この企業・製品について、いま最も大きなニュースは何か**」を、
   軸の制約を付けずに1回検索する
2. その結果を見てから軸を確定する
3. 軸に収まらないものが出てきたら、**軸を広げるか、別枠として記録する**。
   「軸に合わないから捨てる」を絶対にしない

このステップは1対象あたり検索1回で済む。**軸を精緻に設計する前に必ず通すこと。**

**なぜ分離するのか**

同一のモデルは、外部の文章の誤りは高い精度で捕まえるのに、
**自分自身の推論に含まれる同じ誤りは見逃す**（generator-verifier gap）。
自己検証（self-refine）でこの差は埋まらず、
**生成役と判定役を別エージェントに割り当てること**が現時点で最も効く。
検証役には、検証対象の生成過程を見せないほど精度が上がる（fully factored execution）。

**検証は4層に分かれる。層ごとに並列化できるかが違う**

| 層 | 検証すること | 並列化 | 担当 |
|---|---|---|---|
| L1 | URL が実在し到達できるか | **調査と同時に実行可**（主張の中身に依存しない） | 機械（`check-source-urls.mjs`） |
| L2 | その URL に本当にその情報があるか | 主張ごとに並列。調査とは同時不可 | 検証エージェント |
| L3 | 主張の切り出し方が原文と合っているか | **L2 と同一エージェントでよい**（同じページを読むため分けても fetch が増えるだけ） | 検証エージェント |
| L4 | **調査範囲そのものが足りているか** | **全結果が揃うまで判定不能＝バリアが必要** | 独立した批評エージェント |

L1〜L3 は「載せた出典が正しいか」（precision）、L4 は「載せるべきものが漏れていないか」（recall）を見る。
**この2つは逆方向に働く**ため、同じエージェントに両方やらせると双方が弱くなる。L4 は必ず別に立てる。

**過剰検証も事故である**

検証役がドメイン知識を欠くと、**正しい出典まで棄却する**。
文献では検証役の recall が16〜17%まで落ちた例が報告されている。
「疑わしきは削除」に振り切ると、誤報を載せるのと別種の損失が出る。
検証役への指示は「確度を下げる」ではなく **「判定区分を分ける」** 形にする
（CONFIRMED / PLAUSIBLE / REFUTED / UNVERIFIABLE）。

**「到達できない」を「間違っている」と扱わない**

`check-source-urls.mjs` は 404・410・名前解決失敗のみを DEAD として報告し、
403・429・5xx・接続失敗は **INDETERMINATE（判定不能）** に分類する。
実行環境のネットワークポリシーや bot 対策で弾かれているだけの場合があり、
これをリンク切れとして機械的に削除すると**正しい出典を失う**。

**重要**: DEAD が出ても、それは「URL が違う」ことしか意味しない。
2026年8月の実行では DEAD 2件がいずれも「実在する事柄に誤った URL が付いていた」ケースで、
正しい URL に差し替えるだけで済んだ。**DEAD を根拠に記事を取り下げてはならない。**
必ず対象の事実そのものを外部検証してから対応を決める。

**実行環境の制約を先に確認する**

agent 実行環境のネットワークポリシーは、`docs.cursor.com`・`developers.openai.com`・
`code.visualstudio.com`・`github.blog` 等を CONNECT 段階で拒否する。
2026年8月の実測では、930件の出典 URL のうち到達確認できたのは96件のみで、
**828件が判定不能**だった。公式ドキュメントを一次ソースとして要求する調査を組む前に、
`curl -sS "$HTTPS_PROXY/__agentproxy/status"` で拒否ホストを確認すること。
拒否されている場合、その軸の成果は二次ソース依存になるため、
**記事側で「公式ドキュメントで確認できていない」と明記する**か、
ネットワークポリシーを変更した環境で調査し直す。

### review-check.mjs が検出できること・できないこと

`node scripts/review-check.mjs` は22項目を検査する。**コミット前に必ず実行し、エラー0を確認する。**

2026年8月の棚卸で、「データとしては正常だが読者の画面では壊れている」種類の欠陥が
多数見つかったため、以下を追加した（項目18〜22）。

| 項目 | 検出できる問題 | 重大度 |
|---|---|---|
| 18 | `figures` / `coverImage` の画像が実在しない | Error |
| 19 | `tables` / `figures` / `charts` / `embeds` に `afterParagraph` がなく永久に非表示 | Error |
| 20 | 本文に生 Markdown（フェンス単独段落・h1見出し・表・外部リンク）が残っている | Error / Warn |
| 21 | `primarySources` に外部 URL でないものが混入している | Error |
| 22 | review の `rating` が5軸加重平均と 0.25 超ずれている | Warn |

**それでも検出できないこと**（人間の確認が必要）

- **内部リンクの意味的な妥当性**。`?a=` の参照先が存在するかは検査するが、
  **リンクテキストの内容と参照先が対応しているか**は判定できない。
  実際に「GLM-5.2」が別社の記事を、「Hooks」が Hooks に触れていない記事を
  指している例があった
- **記述の事実性**。サイト内に根拠が見当たらないことは、事実の不在を意味しない。
  「Kimi K2.7-Code」「CursorBench の汚染」は当初「裏付けなし」と疑われたが、
  外部確認の結果いずれも実在の事実だった。**訂正の前に必ず外部で検証する**
- **図版の内容が本文と合っているか**。ファイルの存在は検査するが中身は見ない

### 2つの評価尺度の使い分け

サイト内には性質の異なる2つの評価数値が存在する。**同じ「モデル」タブに並んで表示される**ため、
混同させない運用が必要である。

| | モデル比較表（`constants.js` の `MODEL_COMPARISON`） | レビュー記事（`rating` / `ratings`） |
|---|---|---|
| 表示名 | **ベンチマーク総合** | ★（5軸の加重平均） |
| 意味 | 公開ベンチマークに基づく**性能のみ**の総合値 | 性能に加え**料金・提供条件・利用制限**を含む採用判断向けの評価 |
| 算出 | SWE-Bench / Terminal-Bench / OSWorld / BrowseComp / GPQA 等 | AI品質30% / 使いやすさ25% / コスパ20% / 拡張性15% / 企業向け10% |
| 表示 | 数値のみ（★を付けない） | ★＋数値 |

**遵守事項**

- **両者が食い違うのは正常である。** 例: Claude Fable 5 は性能5.0だが、週次50%上限を
  織り込んだレビュー★は3.5。片方に寄せて「統一」してはならない
- モデル比較表には**★を付けない**。★はレビュー記事の語彙であり、視覚的に区別する
- 比較表の `summary` に**期限付きの記述を書かない**（「7/12まで無料」等は期限後に誤情報になる）。
  キャンペーン等に触れる場合は記事側に書く
- どちらかの尺度の意味を変える場合、`BenchScoreExplainer` と `RatingExplainer` の
  両方の説明文を同時に更新する

### 比較記事・料金記事の見直しサイクル

Footer の編集ステートメントで「**主要な比較記事・料金記事は毎月1回以上見直す**」と公言している。
この約束の対象は、記事メタに `reviewCadence: "monthly"` を持つものに限定する。

**月次見直しの対象（12本）**

| 区分 | 対象 |
|---|---|
| ツール横断の料金・比較 | `pricing-comparison-all-tools-2026-march`、`cli-tools-comparison-2026-march`、`editor-comparison-2026-march` |
| 現行世代のモデル | `gpt-5-6-review`、`claude-fable-5-mythos-5-review`、`claude-sonnet-5-review` |
| 主要 CLI | `claude-code`、`openai-codex-agent`、`cursor-cli`、`github-copilot-cli`、`gemini-cli` |
| 主要エディタ | `cursor-editor` |

**選定基準**: 読者が採用・支出の判断に直接使い、かつ陳腐化が速いもの。
新しい世代のモデルが主力になったら、旧世代から `reviewCadence` を外し新世代に付け替える
（例: Opus 5 のレビューを作成したら、そちらに付与する）。

**遵守事項**

- `review-check.mjs` の規則 15b が、対象記事が31日を超えて未更新なら警告する
- 見直しの際は、料金・モデル世代・提供条件が現行と合っているかを確認し、
  本文を更新したうえで `lastReviewed` を更新する。**確認だけして日付を進める運用は禁止**
- **確認範囲に「中核機能が世代交代していないか」を必ず含める。**
  2026年8月の見直しでは、Devin Desktop について運営元とブランド統合は確認したのに、
  **中核だった Cascade が7/1に提供終了していた**ことを見落とし、当日中に訂正した。
  資本関係・料金・製品名だけを見ると、**製品の中身が入れ替わっていても気づかない**
- 対象外の記事にも `lastReviewed` は表示し続ける。ただし頻度は約束しない
- 対象を増減させる場合は Footer の文言と本セクションを同時に更新する

### 訂正・取り下げポリシー

**訂正（Correction）**
- 事実誤認を発見した場合、本文を正しい内容に修正する
- body 末尾の編集履歴に `【訂正 YYYY-MM-DD】（誤）〜 →（正）〜` を記録する
- 誤った記述は残さない（正しい内容に上書き）

**追記（Update）**
- 続報・追加情報がある場合、body に段落を追加する
- 編集履歴に `【追記 YYYY-MM-DD】追加内容の要約` を記録する

**取り下げ（Retraction）**
- 以下の場合に記事を取り下げる:
  1. 主要な事実が虚偽であると判明した場合
  2. ソースが捏造・偽造であると判明した場合
  3. 掲載継続が法的リスクを伴う場合
- 取り下げ手順:
  1. `status: "retracted"` を追加する（`review-check.mjs` が自動検証）
  2. body を取り下げ告知文に置き換える（元の本文は削除）
  3. title の先頭に `【取り下げ】` を追加する
  4. `heroScope` を `"none"` に変更し、`pinned` を削除する
  5. 編集履歴に取り下げ理由を記録する
- 取り下げた記事の id は再利用しない

### 用語集の管理

- 用語集（`vibeCodingGuide.js`）への新用語追加は、記事で **2回以上使用** された用語に限る
- 既存定義の改訂は、公式ドキュメントの変更または業界での用法変化を根拠とする
- 既存用語と重複・包含関係がないことを確認してから追加する

### タグ管理

- 新タグ追加前に既存タグとの重複・包含関係を確認する
- 1回しか使われていないタグは四半期ごとに統廃合を検討する
- 英語が自然な略語（MCP, RAG, API 等）は英語表記、それ以外は日本語に統一する
- 固有名詞タグ（企業名・製品名）は `aiCompanies.js` の `name` / `products` と表記を一致させる

**2026年8月の統廃合で確定した命名規則**

| 種別 | 規則 | 例 |
|---|---|---|
| モデル名 | **ベンダー接頭辞を付けない**。マイナーバージョンは世代でまとめる | `Claude Fable 5` → `Fable 5`、`GPT-5.4/5.5/5.6` → `GPT-5`、`Grok 4.5` → `Grok` |
| 製品名 | `aiCompanies.js` の `products` の正式名を使う | `Copilot` → `GitHub Copilot` |
| 企業名 | 大文字小文字を含め正式表記に揃える | `Nvidia` → `NVIDIA` |
| 日本語／英語 | 同義の英日ペアは**日本語を残す** | `OSS` → `オープンソース`、`Enterprise` → `エンタープライズ` |
| 粒度 | 「X」「X AI」「XAI」のような派生は最も使われている1つに寄せる | `動画`／`動画生成AI` → `動画生成` |

**タグにしないもの**

- **`type` / `heroScope` で表現済みの情報**。`2026年3月`・`特集` 等は記事メタで判別できるため付けない
- **サイト全体に当てはまる語**。`AI` のようなタグは絞り込みの役に立たない
- **期限付きの状態**。`無料プラン有り` のような条件は変化するため、本文に書く

**統廃合の実行手順**

1. `node -e` で `articlesMeta.js` からタグ頻度表を出し、重複候補を洗い出す
2. 旧タグ→新タグの対応表を作り、**`tags` 配列だけ**を対象に置換する。`articlesMeta.js` には
   **1行形式（`"tags": ["a", "b"]`）と複数行形式が混在する**ため、両方を処理すること
   （2026年8月の作業では1行形式を取りこぼし、22件が未変換のまま残った）
3. 置換後に同一記事内でタグが重複しないよう重複除去する
4. `review-check.mjs` と `npm run build` を通し、タグ0件の記事が発生していないことを確認する

### 記事間の依存関係

- `?a=xxx` リンクで参照される記事を更新・取り下げる場合、参照元の記事も確認・更新する
- まとめ記事の元になった速報を訂正した場合、まとめ記事も更新する
- `review-check.mjs` は参照先の存在と取り下げ記事への参照を自動検出する。ただし「元記事の訂正がまとめに反映されたか」は手動確認が必要

## Claude Code Routine 安全ルール

Claude Code Routines（自律実行）でこのリポジトリを操作する場合、以下のルールを**必ず**守ること。

### 記事データへの書き込みは、必ずスクリプトを通す

`src/data/articlesMeta.js` と `src/data/articlesBody.js` を **Write / Edit で直接編集してはならない**。
記事の追加は `node scripts/add-article.mjs <article.json>` を通す。

**なぜ直接編集が危険で、スクリプト経由なら安全なのか。**

危険の本体はファイルが長いことではなく、**モデルが 1.8MB を読んで書き戻すこと**にある。
読み込みが途中で切れれば、そのまま記事が消える。そこで役割を分ける。

| | モデル | スクリプト |
|---|---|---|
| 扱う対象 | 記事**1件ぶん**の JSON | 既存データ全体 |
| 既存データ | 触れない | パースして1件足し、決定論的に書き戻す |

モデルが扱う対象を1件ぶんに限定すれば、入力が長すぎることに起因する欠損は原理的に起きえない。

**同じ「全部書き直す」でも、決定論的なコードがデータを往復させるのと、
モデルが 1.8MB を読んで書き戻すのとでは安全性がまったく違う。**
`scripts/serialize-articles.mjs` が前者を提供し、後者を不要にしている。

`add-article.mjs` は検査に1つでも失敗した場合、**ファイルを一切変更せずに終了する**。
「一部だけ書けた」状態を作らないことが設計の要点である。

### 書き込み禁止（Routine では Read のみ）

- `src/data/aiCompanies.js`
- `src/data/vibeCodingGuide.js`
- `src/constants.js`

これらは記事データと違い追記構造ではなく、設計判断を伴うため対話セッションで扱う。

### 公開まで自律実行する。ただし確信の度合いで経路を分ける

本サイトは**記事の調査・執筆・検証・公開までを AI が自律的に行う運用**に切り替えた。
Routine は検査をすべて通過した記事を `main` に直接 push してよい。

**ただし全件を一律に公開してはならない。** 経路を2つに分ける。

| 経路 | 条件 | 行き先 |
|---|---|---|
| 直接公開 | 一次ソースに**到達して内容を確認できた**。事実関係に未解決の疑問が無い | `main` へ push |
| 下書き PR | 一次ソースに到達できず二次ソース依存である。数値や日付に確信が持てない。**取り下げ・訂正のリスクを感じる** | ブランチ＋draft PR |

**迷ったら PR に回す。** 公開を1日遅らせる損失より、誤報を載せて訂正する損失のほうが大きい。
判断に困ったら PR 側を選ぶこと自体が正しい判断である。

弱い材料しかない記事は**そもそも書かない**。3軸を埋めるために質を落とすくらいなら本数を減らす。
「今日は3本しか出せなかった」は許容される。「5本出したが1本が誤報だった」は許容されない。

公式ドキュメントに到達できないことは頻繁に起きる（実測で930件中828件が判定不能）。
その場合は**記事側にも「公式ドキュメントで確認できていない」と明記する**。

### 公開後の訂正経路

自律公開である以上、誤りは人間のレビューを経ずに世に出る。したがって
**出たあとに直す経路**が実質的な防御線になる。

- 日次点検（`daily-maintenance.yml`、06:40 / 14:40 JST）が記事の消失・期限切れ・
  月次見直しの超過を検出し、必要なときだけ Issue を立てる
- 誤りを見つけた場合は「訂正・取り下げポリシー」に従う。**黙って直さない**
- 記事単位で巻き戻す場合は `git revert <commit>` を使う。
  1日ぶんのコミットは記事ごとに分けて積むこと。まとめて1コミットにすると
  1本だけ取り下げられなくなる

### 自動化の全体像

言語モデルが要るかどうかで実行基盤を分けている。

| 担当 | 内容 | 基盤 | 費用 |
|---|---|---|---|
| 判断の要らない検査 | 記事・フィールドの消失、期限切れ、月次見直しの期限超過、校閲エラー | GitHub Actions（`daily-maintenance.yml`、**06:40 / 14:40 JST**） | 公開リポジトリのため無料 |
| 出典 URL の到達性 | 930件。月曜のみ | GitHub Actions（同上） | 無料 |
| 調査・記事作成・公開 | ニュース調査、執筆、検証、公開 | Claude Code Routine（**07:00 / 15:00 JST の2回**） | サブスクリプション |

Actions 側は「異常なし」を Issue にしない。毎日通知すると無視されるようになるため、
報告すべきことがある場合のみ Issue を立てる。
またワークフロー自体は検知の有無で落とさない。落とすと「壊れた CI」として無視され、
本当に止めるべきときに効かなくなる。公開を止める役割は `deploy.yml` が負っている。

手順書は次にある。

- `scripts/routine-daily-research.md` — 調査のみ（`reports/` に出力）
- `scripts/routine-daily-articles.md` — 記事作成まで（下書き PR を開く）

### フィールドを増やしたときは、キーリストも同時に更新する

記事データに新しいフィールドを足す場合、`scripts/serialize-articles.mjs` の
`META_KEYS` / `BODY_KEYS` と `scripts/split-articles.mjs` の同名の定数を**必ず同時に更新する**。

載せ忘れると、再生成時にそのフィールドが**黙って消える**。
実際 `reviewCadence`（月次見直し対象13本を駆動）と `embeds`（4記事）が漏れており、
どの検査にも掛からない状態だった。`reviewCadence` が消えれば、Footer で公言した
「主要な比較記事・料金記事は毎月1回以上見直す」が無効化される。

`check-article-manifest.mjs` が、データに実在するキーが必ずキーリストに載っていることを
検査する。これは `checks.yml` と `deploy.yml` の両方で走るため、
フィールドが消えた状態では公開まで進まない。

## デイリーニュース調査ルーティン

「今日のニュースある？」「最新ニュースを調べて」等の指示を受けた場合、以下の手順を**必ず**実行する。

### Step 0: 調査クエリの生成

```bash
node scripts/generate-research-queries.mjs --format=markdown --priority=high
```

このスクリプトは `aiCompanies.js`（51社）のプロダクト名と `articlesMeta.js` の頻出タグから、優先度付きの検索クエリリストを自動生成する。**手動でリストを作らず、必ずこのスクリプトを使うこと。**

### Step 1: Tier 1 企業・プロダクトの調査（必須・毎日）

スクリプト出力の Tier 1 クエリを WebSearch で実行する。特に以下を重点チェック：

- **モデルリリース**: 新モデル、バージョンアップ、ベンチマーク結果
- **プロダクトアップデート**: 新機能、料金変更、API 変更
- **GitHub リリース**: 主要 OSS ツールの新バージョン
- **公式ブログ・X 投稿**: 各社公式アカウントの発表

### Step 2: テーマ別トレンド調査（必須・毎日）

ホットタグ（MCP, エージェント, エッジAI, オープンソース等）に関する横断的な検索を行う。

- GitHub Trending（machine-learning, deep-learning カテゴリ）
- Hacker News の AI 関連トップストーリー
- X/Twitter で話題の AI ツール・論文・デモ

### Step 2.5: 公式 org リポジトリの巡回（必須・毎日）

企業が GitHub の公式 org で静かに公開するツールは、企業名検索でも GitHub Trending でも
Hacker News でも拾えないことがある。以下の org の新規・更新リポジトリを毎日確認する。

- `github.com/microsoft/*`、`github.com/openai/*`、`github.com/anthropics/*`
- `github.com/googleapis/*`、`github.com/google-deepmind/*`、`github.com/meta-llama/*`
- `github.com/modelcontextprotocol/*`

**実例**: `microsoft/skill-recorder`（2026年8月公開、画面録画からエージェントのスキルを生成）は、
Tier 1 企業横断・GitHub Trending・Hacker News のいずれの系統でも検出できず、ユーザーの
指摘で初めて把握した。スター数が伸びる前でも、公式 org からの公開は記事価値が高い。

### Step 3: Tier 2〜3 企業の調査

- Tier 2（記事2〜4本の企業）: 毎日軽くチェック
- Tier 3（記事1本以下の企業）: 週1回（月曜）のみ

### Step 4: ニュース価値の判断

以下の基準で記事化を判断する：

| 記事化する | 記事化しない（速報まとめの一項目にとどめる） |
|---|---|
| AIがユーザー体験や社会に直接影響を与える | AIが裏にいるがメインではない |
| 新モデル・新ツールのリリース | マイナーバグ修正 |
| 料金体系の大幅変更 | UIの微調整 |
| セキュリティ・倫理に関わる問題 | 既報の続報で新情報が少ない |
| OSSの画期的なプロジェクト | スター数だけが話題のプロジェクト |

### Step 5: 記事作成

`scripts/daily-update-prompt.md` の Step 2〜5 に従って記事を作成・校閲・コミットする。

### 編集方針 — 3軸バランス

AI News の差別化は**速報だけでなく、新興発掘と社会分析を組み合わせる**点にある。
1日5記事を目標に、以下の3軸でバランスを取る：

| 枠 | タイプ | type | 内容 | 1日の目安 |
|---|---|---|---|---|
| 速報 | 主要プレイヤーのリリース・アップデート | `news` | OpenAI, Anthropic, Google, Meta 等の大型発表 | 2本 |
| 発掘 | 新興企業・OSS・個人開発の注目プロジェクト | `news` | 他メディアが拾わない GitHub Trending、X で話題の新ツール | 2本 |
| 分析 | 社会的インパクト・規制・倫理の考察 | `feature` | AI×軍事、プライバシー、翻訳業界の価格破壊、雇用への影響等 | 1本 |

**重要な原則:**
- 「大企業だから優先」ではない。新興の小さなプロジェクトにも同等の枠を割く
- 「AIが怖い」という批判記事ではなく、「どういうインパクトを社会に与え、規制はどう進むのか」を分析する
- 速報は他メディアも出すので、AI News では**開発者視点の実用的な解説**を付加価値にする
- 分析記事は速報をベースに後日まとめることも可（編集履歴に明記）

### デイリー目標

- **調査候補**: 最大20件のニュース候補を収集する
- **重複チェック**: `articlesMeta.js` の既存記事タイトル・ID・newsDate と照合し、既報を除外する
- **最終出力**: 最低5記事/日を目標とする（ニュースが少ない日は3記事以上）
- **候補リスト提示**: 記事作成前に候補一覧をユーザーに提示し、3軸のバランスを確認する
- **3軸チェック**: 速報ばかり、発掘ばかりに偏っていないか毎日確認する

### 注意事項

- **スクリプトを使わず手動で「主要6社」だけ調べるのは禁止。** 51社＋タグの網羅的調査を行うこと
- 調査結果が0件でも、「調べたが特筆すべきニュースなし」と報告すること
- ユーザーが X で見つけたニュースがこのルーティンで拾えなかった場合、aiCompanies.js やタグに追跡対象を追加すべきか検討すること
- 既存記事と同じトピックでも**新しい切り口や重要な続報**がある場合は、追記または新規記事として扱う

## アクセス解析ポリシー

本サイトは **Cloudflare Web Analytics** による集計のみのアクセス解析を導入している（`src/analytics.js`）。

### 収集する / しないもの

| 収集する（集計値のみ） | 収集しない |
|---|---|
| ページビュー数・人気記事 | 個人を特定する情報 |
| 参照元（X / Google / 直接） | Cookie による追跡 |
| 国・地域 | IP アドレスの保存 |
| デバイス種別・ブラウザ | 個人の行動履歴の紐付け |

### 遵守事項

- **Cookie を使う解析ツール（Google Analytics 等）を追加しない。** 追加は同意バナーの実装義務を発生させ、サイトの方針と衝突する
- **Footer のプライバシー記載と実装を常に一致させる。** `analyticsEnabled` による条件分岐で、解析が無効な間は「一切収集しない」と表示される設計になっている。解析の仕様を変更したら Footer の文言も必ず同時に更新する
- **解析ツールを追加・変更する場合は、必ず branch → PR で行い、CSP と Footer 文言をセットで更新する**
- `CF_BEACON_TOKEN`（`VITE_CF_BEACON_TOKEN`）は Google Analytics の測定 ID と同種の**公開サイト識別子**であり、認証情報ではない。ページのソースに出力される前提の値のため、「認証情報の保護」ポリシーの対象外とする
- トークン未設定時は beacon を読み込まない（外部通信ゼロ）実装を維持する

## 記事内の画像・埋め込みポリシー

記事データは以下の埋め込み手段をサポートする（`ArticleDetail.jsx` が描画）。

| フィールド | 用途 | 著作権上の安全度 |
|---|---|---|
| `embeds` (`type: "x"`) | X 公式投稿の埋め込み | **高** — X の利用規約が認める方法。画像は X 側から配信される |
| `embeds` (`type: "youtube"`) | YouTube 埋め込み（`youtube-nocookie`） | **高** — 公式の埋め込み機能 |
| `charts` | 数値データから自前で描画する図表 | **高** — 数値自体は事実であり著作物ではない。出典明示は必須 |
| `tables` | 比較表 | **高** — 同上 |
| `figures` | 画像（`public/articles/*.svg` 等） | 自作素材は安全。外部画像は下記の制約に従う |

### 遵守事項

- **他社の公式ベンチマーク画像・比較図を紹介したい場合は、原則 `embeds` の X 公式投稿埋め込みを使う。** 画像を自サーバに複製しない
- `figures` に外部の画像 URL を直接指定する場合は、著作権法32条の引用要件（主従関係・引用の必然性・出典明示・明瞭区別性）を満たすか個別に検討し、満たさない場合は使用しない
- **公式発表の数値は事実であり著作物ではない**ため、`charts` / `tables` で自前作図するのが最も安全かつ AI News の差別化になる。必ず `primarySources` に出典を記載する
- X 埋め込みは `dnt: true`（Do Not Track）を維持する

## デプロイワークフロー

### Artifact プレビュー確認済み記事は main に直接 push してよい

対話セッションで作成したニュース記事は、Artifact でプレビュー確認済みなら **`main` ブランチに直接 push** してよい。従来の branch → PR → merge の3段階を Artifact 確認で代替する運用に移行済み。

**直接 main push が許可される変更**:
- 記事追加・更新（`articlesMeta.js`, `articlesBody.js`）
- 記事に付随する `public/feed.xml`, `public/sitemap.xml` の再生成結果
- 記事関連の軽微な CLAUDE.md 更新（用語集、タグ運用等）

**引き続き branch → PR が必須の変更**:
- スクリプト（`scripts/*`）の追加・変更
- コンポーネント（`src/components/*`）の追加・変更
- 設計・データ構造の変更（`aiCompanies.js`, `vibeCodingGuide.js`, `constants.js` 等）
- CLAUDE.md のポリシー変更（本セクションのようなワークフロー変更含む）
- 依存パッケージの追加・削除

**運用ルール**:
- main への直接 push は Artifact プレビューで内容確認済みの記事に限定する
- push 前に `node scripts/review-check.mjs` を必ず実行し、エラーなしを確認する
- push 前に `node scripts/generate-feed.mjs` と `node scripts/generate-sitemap.mjs` を実行して feed/sitemap を最新化する
- ロールバックが必要になった場合は `git revert` で対応する

## プロジェクト構成

- React + Vite で構築された静的サイト
- 記事データ: `src/data/aiToolsData.js`（`articlesMeta.js` + `articlesBody.js` に分割管理）
- デプロイ: main ブランチへの push（直接 or PR merge 経由）で GitHub Pages に自動デプロイ
- 言語: 日本語
